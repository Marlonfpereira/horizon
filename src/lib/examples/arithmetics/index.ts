import { immediates } from "./immediates";
import { temporaries } from "./temporaries";
import { temporaries_and_immediates } from "./temporaries-and-immediates";

export const arithmetics = {
	immediates,
	temporaries,
	temporaries_and_immediates,
};
